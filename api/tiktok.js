export default async function handler(req, res) {
  res.json({ ok:true, mode:'Planificateur interne V2. Publication automatique TikTok à faire plus tard via API officielle, sans stocker les mots de passe.' });
}
